# 🏗️ 建筑行业 AI 智能助手 — 项目架构与文件详解

> 基于 RAG（检索增强生成）的土木工程规范问答系统

---

## 一、系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             👤 用户 (浏览器)                                 │
│                          http://localhost:3000                               │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │
                                  │  ① 用户输入问题
                                  │  POST /api/chat/stream
                                  │  {"message": "混凝土强度等级怎么选？"}
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          🌐 前端 (Frontend)                                  │
│                      React 18 + Vite 5 | 端口 3000                          │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐   │
│  │  App.jsx     │  │ ChatWindow   │  │ MessageBubble│  │  SourceCard   │   │
│  │  (根组件)    │  │ (聊天窗口)   │  │ (消息气泡)   │  │ (引用卡片)    │   │
│  └──────────────┘  └──────────────┘  └──────────────┘  └───────────────┘   │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────────────┐   │
│  │  chat.js     │  │  files.js    │  │  FilePanel / FileUploadZone      │   │
│  │  (API 封装)  │  │  (文件 API)  │  │  (文件管理界面)                  │   │
│  └──────────────┘  └──────────────┘  └──────────────────────────────────┘   │
│                                                                              │
│  Vite 开发服务器自动代理 /api/* → http://localhost:8000                       │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │
                                  │  HTTP SSE (Server-Sent Events) 流式传输
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ⚙️ 后端 (Backend)                                   │
│                      FastAPI + Uvicorn | 端口 8000                           │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                        API 层 (api/)                                  │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐               │   │
│  │  │  routes.py   │  │  schemas.py  │  │  files.py     │               │   │
│  │  │  路由定义    │  │  数据模型    │  │  文件管理API  │               │   │
│  │  └──────┬───────┘  └──────────────┘  └───────────────┘               │   │
│  └─────────┼────────────────────────────────────────────────────────────┘   │
│            │                                                                  │
│            ▼                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                      RAG 引擎 (rag/)                                  │   │
│  │                                                                       │   │
│  │   ② 用户问题                                   ⑥ 生成最终答案         │   │
│  │      │                                              ▲                 │   │
│  │      ▼                                              │                 │   │
│  │  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐          │   │
│  │  │embedding │   │indexing  │   │reranker  │   │  llm.py  │          │   │
│  │  │ 向量化   │──▶│  检索    │──▶│  重排序  │──▶│ DeepSeek │          │   │
│  │  │          │   │ top_k=10 │   │ top_n=5  │   │  生成    │          │   │
│  │  └──────────┘   └──────────┘   └──────────┘   └──────────┘          │   │
│  │       ▲                                          │                   │   │
│  │       │                              ┌───────────┴──────────┐        │   │
│  │       │                              │     prompts.py       │        │   │
│  │       │                              │  系统提示词 + 上下文  │        │   │
│  │       │                              └──────────────────────┘        │   │
│  │       │                                                              │   │
│  │  ┌────────────────────────────────────────────┐                      │   │
│  │  │            query.py (查询编排)             │                      │   │
│  │  │   协调 embedding → indexing → reranker     │                      │   │
│  │  │   → llm 的全流程，支持流式/推理链模式      │                      │   │
│  │  └────────────────────────────────────────────┘                      │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │config.py │  │  main.py     │  │file_parser.py│  │ user_index.py    │   │
│  │ 全局配置 │  │ FastAPI入口  │  │ 文件解析     │  │ 用户专属索引     │   │
│  └──────────┘  └──────────────┘  └──────────────┘  └──────────────────┘   │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │
                                  │  ③ Embedding 查询 & ④ 检索
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        📚 知识库 & 向量索引                                   │
│                                                                              │
│  ┌─────────────────────────────────┐  ┌────────────────────────────────┐    │
│  │   backend/storage/              │  │  backend/src/data/documents/    │    │
│  │   向量索引 (FAISS)              │  │  原始文档 (.txt)                │    │
│  │                                 │  │                                │    │
│  │   文档 → 切块 → Embedding       │  │  国家标准GB/                    │    │
│  │   → 向量存储 (持久化到磁盘)     │  │    ├── 结构设计/ (17项)         │    │
│  │                                 │  │    ├── 施工验收/ (14项)         │    │
│  └─────────────────────────────────┘  │    ├── 安全规范/ (10项)         │    │
│                                       │    ├── 材料标准/ (12项)         │    │
│                                       │    └── 检测与试验/ (6项)        │    │
│                                       │  行业标准/                       │    │
│                                       │    └── JGJ建筑行业/ (15项)      │    │
│                                       │  法律法规/ (3项)                 │    │
│                                       │  技术规程/                       │    │
│                                       │  地方标准/                       │    │
│                                       └────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                  │
                                  │  ⑤ LLM API 调用 (DeepSeek)
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ☁️ 外部 AI 服务                                     │
│                                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐                         │
│  │  DeepSeek API        │  │  ModelScope API      │                         │
│  │  (LLM 大语言模型)    │  │  (Embedding/Rerank)  │                         │
│  │  deepseek-chat       │  │  Qwen3-Embedding-8B  │                         │
│  │  deepseek-reasoner   │  │  Qwen3-Reranker-8B   │                         │
│  └──────────────────────┘  └──────────────────────┘                         │
└─────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                           📊 数据流总结 (一次问答的完整旅程)
═══════════════════════════════════════════════════════════════════════════════

  用户输入问题
       │
       ▼
  ① 前端 ChatWindow.jsx 捕获输入 → chat.js 发 SSE 请求 → Vite 代理转发
       │
       ▼
  ② 后端 routes.py 接收请求 → query.py 启动查询流程
       │
       ▼
  ③ embedding.py 把问题转成向量 → 在向量索引中找最相似的 10 条文档
       │
       ▼
  ④ reranker.py 对 10 条结果重新打分排序 → 精选最相关的 5 条
       │
       ▼
  ⑤ prompts.py 构造提示词 (系统角色 + 5条文档 + 用户问题) → llm.py 发给 DeepSeek
       │
       ▼
  ⑥ DeepSeek 逐字生成回答 → SSE 流式返回 → 前端逐字显示
       │
       ▼
  ⑦ MessageBubble.jsx 渲染答案 + SourceCard.jsx 展示引用来源
```

---

## 二、完整数据流（一次问答的旅程）

```
用户输入："混凝土强度等级怎么选？"
  │
  ▼
① 前端 ChatWindow.jsx 捕获输入
  → chat.js 封装 HTTP SSE 请求
  → Vite 代理转发 /api/* → localhost:8000
  │
  ▼
② 后端 routes.py 接收 POST /api/chat/stream
  → schemas.py 校验请求格式
  → 调用 query.py 启动查询流程
  │
  ▼
③ embedding.py 把用户问题转成向量(数字)
  → indexing.py 在向量索引中找最相似的 Top 10 条文
  │
  ▼
④ reranker.py 对 Top 10 重新打分排序
  → 精选最相关的 Top 5 条规范条文
  │
  ├─ 备选路径：USE_REASONING=true 时
  │  → astream_query_reasoning() 直连 DeepSeek
  │  → 输出 thinking (思维链) + token (答案)
  │
  ▼
⑤ prompts.py 构造完整提示词：
  "你是土木工程专家... [5条相关规范条文] ...用户问题：混凝土等级？"
  → llm.py 通过 OpenAI 兼容接口发给 DeepSeek
  │
  ▼
⑥ DeepSeek 逐字生成回答
  → SSE (Server-Sent Events) 流式返回
  → 前端 ChatWindow.jsx 逐字显示
  │
  ├─ event: thinking → MessageBubble.jsx 琥珀色折叠面板
  ├─ event: token    → 逐字追加到回答区域
  ├─ event: source   → SourceCard.jsx 展示引用来源
  └─ event: done     → 完成，折叠思考过程
  │
  ▼
⑦ 用户看到完整答案 + 引用来源卡片
  "根据《混凝土结构设计规范》(GB 50010-2010)第4.1.2条..."
```

---

## 三、技术架构总览

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **前端框架** | React 18 + Vite 5 | 单页应用，开发端口 3000 |
| **UI 设计** | Inter 字体 + Slate 色系 | Swiss-Style 极简设计系统 |
| **Markdown** | marked 库 | 支持表格/代码块/列表/加粗斜体 |
| **后端框架** | FastAPI + Uvicorn | 异步 Python Web 框架，端口 8000 |
| **RAG 框架** | LlamaIndex | 文档加载、索引构建、查询编排 |
| **大语言模型** | DeepSeek (deepseek-chat) | 通过 OpenAI 兼容 API 接入 |
| **推理模型** | DeepSeek (deepseek-reasoner) | 输出思维链 (Chain of Thought) |
| **Embedding** | BAAI/bge-small-zh-v1.5 (本地) | 文本→向量，离线可用 |
| **Embedding (备选)** | Qwen3-Embedding-8B (云端) | ModelScope API |
| **Reranker** | Qwen3-Reranker-8B | ModelScope API，失败自动降级 |
| **向量存储** | FAISS (LlamaIndex 内置) | 持久化到 backend/storage/ |
| **PDF 解析** | PyMuPDF | PDF→TXT 文字提取 |
| **文档切分** | SentenceSplitter | 按 512 字/块切分，重叠 64 字 |

---

## 四、文件详细说明

### 📁 根目录 (项目入口)

| 文件 | 类型 | 作用（大白话） |
|------|------|---------------|
| `.env` | 配置 | **密码本**。存放 DeepSeek API 密钥、ModelScope 密钥、端口号等敏感信息。这个文件不能上传到 GitHub，每个人自己本地配一份 |
| `.env.example` | 配置模板 | **密码本模板**。把 `.env` 里需要填写的项目列出来（但不包含真实密码），方便新成员照着填 |
| `.gitignore` | Git 配置 | **上传黑名单**。告诉 Git 哪些文件/文件夹不要上传（node_modules、.env、__pycache__、storage/ 等） |
| `CLAUDE.md` | 项目文档 | **给 AI 编程助手的说明书**。记录了这个项目的技术栈、架构、常用命令，让 AI 助手理解项目时不用每次都从头读代码 |
| `规范清单-200项.csv` | 数据清单 | **采购清单**。一张 CSV 表格，列出 200 项建筑规范的标准编号、名称、分类、优先级（S/A/B/C）、官方来源链接。是整个数据管线的"唯一真源" |

---

### 📁 frontend/ — 前端（用户看到的网页界面）

#### 配置文件

| 文件 | 作用（大白话） |
|------|---------------|
| `package.json` | **购物清单**。记录项目依赖了哪些 npm 包（React、Vite、marked 等）以及版本范围。别人 clone 项目后运行 `npm install` 就按这个清单安装 |
| `package-lock.json` | **精确购物清单**。锁定每个依赖包的确切版本号（包括间接依赖的依赖），确保所有人安装的版本完全一致 |
| `index.html` | **网页入口**。浏览器打开的第一页，`<div id="root">` 是 React 挂载的容器，`<script>` 加载 JS 主文件 |
| `vite.config.js` | **开发服务器设置**。配置 Vite 打包工具的行为，最重要的是设置代理：前端 3000 端口收到的 `/api/*` 请求自动转发到后端 8000 端口 |

#### 源代码 `frontend/src/`

| 文件 | 作用（大白话） |
|------|---------------|
| `main.jsx` | **开机键**。React 应用的入口文件，把 `<App />` 组件渲染到 `index.html` 的 `<div id="root">` 里 |
| `App.jsx` | **主页面**。整个应用的最顶层组件，决定页面结构：顶部标题栏（含 API 连接状态指示灯）+ 中间聊天主区域 |
| `App.css` | **全局样式**。定义整个网站的颜色（Slate 灰蓝色系）、字体（Inter 无衬线字体）、间距、布局等视觉风格。遵循 Swiss-Style 极简设计 |
| `api/chat.js` | **对话 API 封装**。和服务器通信的三个功能函数：①`healthCheck()` 检查后端是否在线 ②`sendMessage()` 非流式问答 ③`sendMessageStream()` 流式问答（用 SSE 接收逐字输出） |
| `api/files.js` | **文件 API 封装**。封装文件上传/下载/管理等 API 请求 |
| `components/ChatWindow.jsx` | **聊天窗口**。整个对话界面的核心组件：显示历史消息列表、处理流式输入（thinking/token/source/done 四种事件）、建议问题快捷按钮、底部输入框 |
| `components/MessageBubble.jsx` | **消息气泡**。渲染单条消息（用户或 AI）。使用 `marked` 库把 Markdown 转成富文本（表格/代码块/加粗/斜体/列表）。如果 AI 有思考过程，显示为琥珀色的折叠面板（推理时自动展开，完成后自动折叠） |
| `components/SourceCard.jsx` | **引用来源卡片**。展示 AI 回答引用的规范来源：标准编号（如 GB 50010-2010）、章节号（如 4.1.2）、条文原文摘要 |
| `components/FilePanel.jsx` | **文件管理面板**。侧边栏或弹窗界面，展示和管理用户已上传的文件列表 |
| `components/FileUploadZone.jsx` | **文件上传区**。拖拽或点击上传文件的交互组件 |

---

### 📁 backend/ — 后端（服务器处理逻辑）

#### 配置文件

| 文件 | 作用（大白话） |
|------|---------------|
| `requirements.txt` | **Python 购物清单**。列出所有需要的 Python 库：FastAPI、uvicorn、llama-index、httpx、PyMuPDF、sentence-transformers 等 |

#### 主程序 `backend/src/`

| 文件 | 作用（大白话） |
|------|---------------|
| `main.py` | **服务器开机键**。创建 FastAPI 应用实例，配置 CORS（只允许前端 localhost:3000 访问），注册所有 API 路由，用 uvicorn 启动服务器。`APP_ENV=development` 时开启 Swagger 文档页 `/docs` |
| `config.py` | **全局配置中心**。从 `.env` 文件读取所有设置项：API 密钥、模型名称、chunk 大小(512)、检索数量(top_k=10)、Embedding 模式(local/api) 等，集中管理，其他文件从这里获取配置 |

#### API 层 `backend/src/api/`

| 文件 | 作用（大白话） |
|------|---------------|
| `routes.py` | **路由定义（核心）**。定义所有对外 API 接口：①`GET /api/health` 健康检查 ②`POST /api/chat` 非流式问答 ③`POST /api/chat/stream` 流式问答（SSE）。索引懒加载（第一次调用时才构建索引）。根据 `USE_REASONING` 开关选择推理链或标准模式 |
| `schemas.py` | **数据模板**。用 Pydantic 定义请求和响应的格式规范：`ChatRequest`（输入格式）、`ChatResponse`（输出格式）、`SourceDocument`（引用来源格式）等，确保前后端数据格式不会乱 |
| `files.py` | **文件管理接口**。处理文件上传、列表查看、删除等操作的后端 API 路由 |

#### RAG 引擎 `backend/src/rag/`（AI 大脑）

| 文件 | 作用（大白话） |
|------|---------------|
| `llm.py` | **大脑连接器**。封装对大语言模型的调用：①`get_llm()` 创建标准对话模型（DeepSeek-Chat，max_tokens=2048）②`get_reasoning_llm()` 创建推理模型（DeepSeek-Reasoner，max_tokens=8192，会输出思考过程）。通过 OpenAI 兼容接口接入 LlamaIndex 框架 |
| `embedding.py` | **文本 → 数字转换器**。把文字段落转成高维向量（一串数字），这样计算机才能做"相似度计算"。双模式：①`LocalEmbedding` 用本地下载的 BAAI/bge-small-zh-v1.5 模型，离线可用 ②`ModelScopeEmbedding` 调用云端 Qwen3-Embedding-8B，需联网 |
| `reranker.py` | **精排器**。第一次检索返回 10 条结果（粗排），Reranker 做精细的二轮打分排序，精选最相关的 5 条。封装 ModelScope Rerank API，失败时自动降级为保留原始排序，确保系统不崩溃 |
| `indexing.py` | **索引构建器**。负责知识库的"消化"工作：读取所有 TXT 文档 → `SentenceSplitter` 按 512 字/块切分（重叠 64 字）→ 调用 Embedding 向量化 → 存入 FAISS 向量索引 → 持久化到 `backend/storage/` |
| `query.py` | **查询编排器（核心引擎）**。这是整个 RAG 流程的"总指挥"：①`get_query_engine()` 非流式查询 ②`get_streaming_query_engine()` 流式查询 ③`query_with_sources()` 带来源的非流式查询 ④`astream_query_reasoning()` 推理链真流式（绕过 LlamaIndex，直连 DeepSeek API，输出 thinking/token/source 事件） |
| `prompts.py` | **角色设定书**。定义发送给 LLM 的提示词模板。包含：①系统提示词（"你是一位资深土木工程专家..."）②QA 模板（要求引用标准编号和章节号③回答格式要求）。相当于给 AI 的"工作手册" |
| `file_parser.py` | **文件解析器**。把用户上传的 PDF/TXT 文件解析成系统能处理的纯文本。PDF 使用 PyMuPDF 提取文字，支持 OCR 识别 |
| `user_index.py` | **个人知识库**。为多用户场景准备的模块——每个用户可以上传自己的文件、建自己的专属索引，实现个人知识库隔离 |

---

### 📁 backend/scripts/ — 数据准备工具

| 文件 | 作用（大白话） |
|------|---------------|
| `01_download.py` | **官网下载器**。从官方来源（flk.npc.gov.cn 法律法规数据库、openstd.samr.gov.cn 国家标准公开系统）下载法律法规和标准信息 |
| `02_setup_knowledge_base.py` | **知识库初始化**。创建文档目录结构（按分类建文件夹）、将 PDF 转为 TXT（PyMuPDF + OCR 支持）、统计知识库收录情况 |
| `03_auto_crawler.py` | **自动爬虫**。从 gf.cabr-fire.com 批量抓取建筑标准全文：搜索→匹配标准编号→提取章节内容→合并保存为 TXT。支持按优先级(S/A/B/C)筛选、断点续传 |
| `build_index.py` | **索引构建工具**。命令行工具，三种模式：①`--build` 首次构建/加载已有索引 ②`--rebuild` 强制重建 ③`--status` 查看索引统计（文档数、chunk 数等） |
| `sources.json` | **URL 小清单**。记录了 14 项规范在官方网站上的下载地址，供 `01_download.py` 使用 |

---

### 📁 backend/src/data/documents/ — 知识库文档

存放所有建筑规范原文，按分类组织：

```
国家标准GB/
├── 结构设计/    混凝土结构、钢结构、抗震、地基、荷载等设计规范 (17项)
├── 施工验收/    施工质量、混凝土、砌体、钢结构等验收规范 (14项)
├── 安全规范/    施工安全、高处作业、临时用电、防火等 (10项)
├── 材料标准/    水泥、钢筋、混凝土、钢材等材料标准 (12项)
└── 检测与试验/  岩土勘察、土工试验、结构检测等 (6项)

行业标准/
└── JGJ建筑行业/  脚手架、机械使用、冬期施工、焊接等 (15项)

法律法规/         建筑法、安全生产法、建设工程质量管理条例 (3项)
技术规程/
地方标准/
```

文件命名规则：`{标准编号}_{名称}.txt`，如 `GB 50010-2010(2015修订)_混凝土结构设计规范.txt`

---

### 📁 backend/storage/ — 向量索引存储

存放 FAISS 向量索引文件（JSON 格式），是知识库文档被"消化"后的产物。文档被切块→向量化→存入索引，查询时直接在这个索引中做相似度搜索，不需要每次重读原文。

---

### 📁 .claude/ — AI 编程助手配置

存放 Claude Code 的工作流技能（skills）和本地设置（`settings.local.json`）。这些是辅助开发的工具配置，不是项目的核心功能代码。主要包括：设计系统技能、品牌设计技能、UI 样式技能、Banner 设计技能等。

---

## 五、关键设计决策（为什么这样做）

| 决策 | 原因 |
|------|------|
| **无数据库** | 知识库全为文本文件，向量索引存为 JSON，无需维护 PostgreSQL/Redis，部署极简 |
| **无认证** | 单用户设计，无需登录/注册/权限管理，降低复杂度 |
| **索引懒加载** | 首次 API 调用时才加载索引，启动速度快，内存按需使用 |
| **Reranker 降级** | ModelScope API 万一挂了，自动回退到原始排序，保证服务不中断 |
| **流式 SSE** | 用 Server-Sent Events 逐字输出，用户体验好（不用等完整答案） |
| **本地 Embedding** | 默认用本地模型 (bge-small-zh-v1.5)，离线也能跑，不依赖云端 API |
| **推理链模式** | 可选开启 DeepSeek-Reasoner，让 AI 先思考再回答，专业问题准确率更高 |
| **Swagger 条件显示** | 开发环境显示 API 文档方便调试，生产环境隐藏保证安全 |

---

## 六、环境变量说明

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DEEPSEEK_API_KEY` | — | **必填**，DeepSeek API 密钥 |
| `DEEPSEEK_MODEL` | `deepseek-chat` | 标准对话模型 |
| `DEEPSEEK_REASONING_MODEL` | `deepseek-reasoner` | 推理模型（输出思维链） |
| `USE_REASONING` | `false` | 是否启用推理链模式 |
| `MODELSCOPE_API_KEY` | — | Embedding/Rerank API 密钥 |
| `EMBEDDING_MODE` | `local` | `local` 离线 / `api` 云端 |
| `LOCAL_EMBEDDING_MODEL` | `BAAI/bge-small-zh-v1.5` | 本地 Embedding 模型 |
| `EMBEDDING_MODEL` | `Qwen/Qwen3-Embedding-8B` | 云端 Embedding 模型 |
| `RERANK_MODEL` | `Qwen/Qwen3-Reranker-8B` | 云端 Rerank 模型 |
| `BACKEND_PORT` | `8000` | 后端端口 |
| `FRONTEND_PORT` | `3000` | 前端端口 |
| `APP_ENV` | `development` | 环境：development / production |

---

*文档生成日期：2026-08-07*
